// The same new-task form, inside the teammate's own URL space. The API
// records the actual creator from their session, not the form default.
export { default, dynamic } from "@/app/(dashboard)/tasks/new/page";
